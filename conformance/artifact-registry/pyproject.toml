[project]
name = "superintendent-registry"
version = "0.2.0"
description = "Content-addressed registry, authority epochs, grants, leases, and reconciliation"
requires-python = ">=3.11"

[project.scripts]
superintendent-registry = "superintendent_registry.cli:main"
