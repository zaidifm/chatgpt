[project]
name = "superintendent-registry"
version = "0.1.0"
description = "Content-addressed artifact and receipt reconciliation prototype"
requires-python = ">=3.11"

[project.scripts]
superintendent-registry = "superintendent_registry.cli:main"
