[project]
name = "superintendent-registry"
version = "0.3.0"
description = "Resident loopback service, content registry, authority epochs, grants, leases, and reconciliation"
requires-python = ">=3.11"

[project.scripts]
superintendent-registry = "superintendent_registry.cli:main"
superintendent-service = "superintendent_registry.service_cli:main"
